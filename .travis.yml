language: objective-c
osx_image: xcode8
xcode_sdk: iphonesimulator10.0

env:
  global:
  matrix:
    - DESTINATION="OS=10.0,name=iPhone 6s"      CONFIGURATION="Debug"    IsIntegrationTestingTask="true"
    - DESTINATION="OS=10.0,name=iPhone 6s"      CONFIGURATION="ReleaseTest"    IsIntegrationTestingTask="false"
before_install:
  - brew install sendemail
script:
  - sendemail -f panzh@tropo.com -t panzh@tropo.com -u subject -m "message" -s smtp.gmail.com -o tls=yes -xu panzh@tropo.com -xp Td?123456

branches:

